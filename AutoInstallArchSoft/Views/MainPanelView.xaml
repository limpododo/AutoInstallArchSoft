<UserControl x:Class="AutoInstallArchSoft.Views.MainPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="120" d:DesignWidth="300" 
             MinWidth="400" >
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        
        <Button Grid.Row="0" Command="{Binding StartInstallCommand}" Content="{StaticResource InstallButtonText}" 
                Style="{DynamicResource AsButtonStyle}" Height="50" Width="250" VerticalAlignment="Center"/>
        <Button Grid.Row="1" Command="{Binding StartCrackCommand}" Content="{StaticResource CrackButtonText}" 
                Style="{DynamicResource AsButtonStyle}" Height="50" Width="250" VerticalAlignment="Center"/>
    </Grid>
    
</UserControl>
